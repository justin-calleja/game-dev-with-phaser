import { Scene, Game } from "./dummy-phaser";

const game = new Game({
  scenes: [
    // TODO: ... add scenes
  ],
  width: 800,
  height: 400,
  parent: document.getElementById("game-container"),
});
