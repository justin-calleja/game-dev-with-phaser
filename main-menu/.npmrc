# pixel-tools' wrapper.js relies on process.argv[1] being a symlink named
# after the tool (e.g. "fontpack"). pnpm's default isolated layout creates
# shell script stubs instead, which breaks the binary name resolution.
node-linker=hoisted
